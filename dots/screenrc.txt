# Escape Key
escape ^kk

# to use 256 colors
defbce on 
term screen-256color
shell bash
termcapinfo xterm*|screen* Co#256:pa#32767:AB=\E[48;5;%dm:AF=\E[38;5;%dm
shelltitle "$ |bash"

vbell off
autodetach on
startup_message off
msgwait 5
msgminwait 1
defscrollback 30000
mousetrack off

# colors
sorendition '= Kw'
#hardstatus alwayslastline '%= %{= Rk} %2` %{-} [%{= dr}%1`%{-}] %{=u dy}%m/%d %02c:%s%{-} '
hardstatus ignore
caption always '%?%F%{=b KW}%:%{= Kw}%?%n. %t'

# resize
bind < resize -1
bind > resize +1
bind H resize -5
bind L resize +5

# mov
bind ^h focus left
bind ^j focus down
bind ^k focus up
bind ^l focus right

# command!
bind t screen
bind g screen www
bind w screen vimwiki
bind m screen vimail
bind s screen salmon

# layout
layout new
layout select 0
split -v

# vim: set ft=screen:
